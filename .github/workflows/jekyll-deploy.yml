name: Node.js CI
on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set-up Ruby
        uses: actions/setup-node@v1
        with:
          node-version: "18.x"
          ruby-version: '3.3.22' # Not needed with a .ruby-version file
          bundler-cache: true # runs 'bundle install' and caches installed gems automatically
        
      - run: sudo gem install bundler && sudo bundle install
      - run: npm install
      - run: npm run build
      - name: Jekyll Deploy Action
        uses: jeffreytse/jekyll-deploy-action@v0.4.0
        with:
          provider: 'github'
          token: "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" # It's your Personal Access Token(PAT)
        env:
          JEKYLL_ENV: production
          GH_PAGES_TOKEN: "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"
